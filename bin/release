#!/usr/bin/env bash
# bin/release <build-dir>

echo -e "---\ndefault_process_types:\n  web: varify ./nginx.conf \$HOME/modules \$DEPS_DIR/\$DEPS_IDX/nginx/nginx/modules && nginx -p \$PWD -c ./nginx.conf"
